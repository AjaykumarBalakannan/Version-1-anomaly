# requirements.txt

pandas>=1.5.0
numpy>=1.22.0
openpyxl>=3.0.0     # For reading/writing Excel files (.xlsx)
pyarrow>=8.0.0      # For reading Parquet files with pandas
fastparquet>=0.8.0  # Optional: alternative Parquet support for pandas
tqdm>=4.60.0        # For progress bars
pymongo>=4.0.0      # For MongoDB operations
psutil>=5.8.0       # For memory monitoring

# Standard library modules like 'logging', 'os', 're' do not need to be listed, will be pre-installed

# Anomaly Detection V1 dependencies
pycaret>=3.0.0      # For advanced anomaly detection models
pyod>=1.0.0         # For additional anomaly detection algorithms
scikit-learn>=1.0.0 # Required by PyCaret and PyOD

# Visualization dependencies
matplotlib>=3.5.0   # For plotting and visualization

